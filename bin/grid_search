#!/bin/bash
# dump grid_search.py output into a temporary drakefile and run it
DIR="$(dirname "$(readlink -f "$0")")"
TMPFILE=`mktemp` || exit 1
params=$(python $DIR/grid_search.py $TMPFILE "$@") || exit
drake -w $TMPFILE $params
